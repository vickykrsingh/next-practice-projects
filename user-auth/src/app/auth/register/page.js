import { RegisterForm } from "@/components/client-comp/RegisterForm";

export default function registerPage(){
    return (
        <main className="w-full min-h-[90vh] flex items-center justify-center bg-slate-700 ">
            <RegisterForm/>
        </main>
    )
}